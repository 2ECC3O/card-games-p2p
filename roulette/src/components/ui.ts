/** Shared controls inherit each game's card-room accent. */
const base = 'room-button';
export const button = {primary: `${base} room-primary`, secondary: `${base} room-secondary`, quiet: `${base} room-quiet`, danger: `${base} room-danger`};
export const field = 'room-field';
export const label = 'room-label';
